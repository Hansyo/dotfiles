# 色を使用できるようにする
autoload -Uz colors
colors

# プロンプトの設定
## git-promptの呼び出し
source ~/git/zsh-git-prompt/zshrc.sh
## 実際の見た目
PROMPT='
path %d
%F{blue}${VIRTUAL_ENV##*/}%f%#%F{cyan}%n%f%(?.%F{white}$%f.%F{yellow}[%?]%f%F{red}$%f) '
RPROMPT='$(git_super_status) '
# virtualenvでpromptを変更しない
export VIRTUAL_ENV_DISABLE_PROMPT=1
# lsに色付けをする
# setopt CLICOLOR=true
