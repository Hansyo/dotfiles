" conceal(勝手にtexの記法をマルチバイト文字にするやつ)を無効化
let g:tex_conceal=''
let g:vimtex_syntax_conceal_disable = 1

" 折りたたみの最小単位を変更?
"let g:vimtex_fold_envs = []

" vimtexのビューアーを変更
let g:vimtex_view_general_viewer = 'zathura'

" 起動時にいろいろ言われるので、設定
let g:tex_flavor = 'latex'

" quicklanはどうすべきか...
